<!DOCTYPE html>
<html lang="az">
<head>
    <meta charset="UTF-8">
    <title>Depo Paneli</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style>
        .nav-pills .nav-link.active { background-color: #198754; }
    </style>
</head>
<body class="bg-light">

<div class="container py-4">
    <div class="d-flex justify-content-between align-items-center mb-4">
        <h2 class="text-success">📦 Depo İdarəetmə Paneli</h2>
        <div>
            <a href="/" class="btn btn-outline-secondary me-2">🏠 Ana Menyu</a>
            <a href="/download/excel/depo" class="btn btn-success">📊 Excel Hesabatı Yüklə (.xlsx)</a>
        </div>
    </div>

    <!-- 4 Alt Bölmə Navigasiyası -->
    <ul class="nav nav-pills nav-justified mb-4" id="depoTabs" role="tablist">
        <li class="nav-item">
            <button class="nav-link active" id="yararsiz-tab" data-bs-toggle="pill" data-bs-target="#yararsiz">⚠️ Yararsız</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" id="adsiz-tab" data-bs-toggle="pill" data-bs-target="#adsiz">🏷️ Adsız Palet</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" id="qaytarma-tab" data-bs-toggle="pill" data-bs-target="#qaytarma">↩️ Qaytarma</button>
        </li>
        <li class="nav-item">
            <button class="nav-link" id="grzone-tab" data-bs-toggle="pill" data-bs-target="#grzone">📍 GR-ZONE</button>
        </li>
    </ul>

    <div class="tab-content" id="depoTabsContent">
        
        <!-- YARARSIZ -->
        <div class="tab-pane fade show active" id="yararsiz">
            <div class="card border-danger shadow-sm">
                <div class="card-header bg-danger text-white fw-bold">Yararsız Məhsul Növbəsi (YAR)</div>
                <div class="card-body">
                    <form action="/depo/add/yararsiz" method="POST" class="row g-2 mb-3">
                        <div class="col-md-9">
                            <input type="text" name="details" class="form-control" placeholder="Məhsul adı və zədə haqqında qeyd..." required>
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-danger w-100">+ YAR Növbəsi Al</button>
                        </div>
                    </form>
                    <ul class="list-group">
                        {% for item in yararsiz_list %}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><b>#{{ item.code }}</b> - {{ item.details }} <small class="text-muted">({{ item.Tarix }})</small></span>
                            <span class="badge bg-warning text-dark">{{ item.status }}</span>
                        </li>
                        {% else %}
                        <li class="list-group-item text-muted">Aktiv növbə yoxdur.</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>

        <!-- ADSIZ PALET -->
        <div class="tab-pane fade" id="adsiz">
            <div class="card border-secondary shadow-sm">
                <div class="card-header bg-secondary text-white fw-bold">Adsız Palet Növbəsi (ADS)</div>
                <div class="card-body">
                    <form action="/depo/add/adsiz" method="POST" class="row g-2 mb-3">
                        <div class="col-md-9">
                            <input type="text" name="details" class="form-control" placeholder="Palet sayı və müvəqqəti təsvir..." required>
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-secondary w-100">+ ADS Növbəsi Al</button>
                        </div>
                    </form>
                    <ul class="list-group">
                        {% for item in adsiz_list %}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><b>#{{ item.code }}</b> - {{ item.details }} <small class="text-muted">({{ item.Tarix }})</small></span>
                            <span class="badge bg-warning text-dark">{{ item.status }}</span>
                        </li>
                        {% else %}
                        <li class="list-group-item text-muted">Aktiv növbə yoxdur.</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>

        <!-- QAYTARMA -->
        <div class="tab-pane fade" id="qaytarma">
            <div class="card border-primary shadow-sm">
                <div class="card-header bg-primary text-white fw-bold">Qaytarma Növbəsi (QAY)</div>
                <div class="card-body">
                    <form action="/depo/add/qaytarma" method="POST" class="row g-2 mb-3">
                        <div class="col-md-9">
                            <input type="text" name="details" class="form-control" placeholder="Qaytarılan faktura/qaimə no..." required>
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-primary w-100">+ QAY Növbəsi Al</button>
                        </div>
                    </form>
                    <ul class="list-group">
                        {% for item in qaytarma_list %}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><b>#{{ item.code }}</b> - {{ item.details }} <small class="text-muted">({{ item.Tarix }})</small></span>
                            <span class="badge bg-warning text-dark">{{ item.status }}</span>
                        </li>
                        {% else %}
                        <li class="list-group-item text-muted">Aktiv növbə yoxdur.</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>

        <!-- GR-ZONE -->
        <div class="tab-pane fade" id="grzone">
            <div class="card border-info shadow-sm">
                <div class="card-header bg-info text-white fw-bold">GR-ZONE Növbəsi (GRZ)</div>
                <div class="card-body">
                    <form action="/depo/add/grzone" method="POST" class="row g-2 mb-3">
                        <div class="col-md-9">
                            <input type="text" name="details" class="form-control" placeholder="Daxil olan yük və maşın nömrəsi..." required>
                        </div>
                        <div class="col-md-3">
                            <button type="submit" class="btn btn-info text-white w-100">+ GRZ Növbəsi Al</button>
                        </div>
                    </form>
                    <ul class="list-group">
                        {% for item in grzone_list %}
                        <li class="list-group-item d-flex justify-content-between align-items-center">
                            <span><b>#{{ item.code }}</b> - {{ item.details }} <small class="text-muted">({{ item.Tarix }})</small></span>
                            <span class="badge bg-warning text-dark">{{ item.status }}</span>
                        </li>
                        {% else %}
                        <li class="list-group-item text-muted">Aktiv növbə yoxdur.</li>
                        {% endfor %}
                    </ul>
                </div>
            </div>
        </div>

    </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>